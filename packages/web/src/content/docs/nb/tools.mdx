---
title: VerktÃ¸y
description: Administrer verktÃ¸yene en LLM kan bruke.
---

VerktÃ¸y lar LLM utfÃ¸re handlinger i kodebasen din. OpenCode kommer med et sett med innebygde verktÃ¸y, men du kan utvide det med [egendefinerte verktÃ¸y](/docs/custom-tools) eller [MCP servere](/docs/mcp-servers).

Som standard er alle verktÃ¸y **aktivert** og trenger ikke tillatelse for Ã¥ kjÃ¸re. Du kan kontrollere verktÃ¸yets oppfÃ¸rsel gjennom [tillatelser](/docs/permissions).

---

## Konfigurasjon

Bruk feltet `permission` for Ã¥ kontrollere verktÃ¸yets oppfÃ¸rsel. Du kan tillate, avslÃ¥ eller kreve godkjenning for hvert verktÃ¸y.

```json title="opencode.json"
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "edit": "deny",
    "bash": "ask",
    "webfetch": "allow"
  }
}
```

Du kan ogsÃ¥ bruke jokertegn for Ã¥ kontrollere flere verktÃ¸y samtidig. For eksempel, for Ã¥ kreve godkjenning for alle verktÃ¸y fra en MCP-server:

```json title="opencode.json"
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "mymcp_*": "ask"
  }
}
```

[Finn ut mer](/docs/permissions) om konfigurering av tillatelser.

---

## Innebygd

Her er alle de innebygde verktÃ¸yene som er tilgjengelige i OpenCode.

---

### bash

UtfÃ¸r skallkommandoer i prosjektmiljÃ¸et ditt.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "bash": "allow"
  }
}
```

Dette verktÃ¸yet lar LLM kjÃ¸re terminalkommandoer som `npm install`, `git status` eller en hvilken som helst annen skallkommando.

---

### edit

Endre eksisterende filer ved Ã¥ bruke eksakte strengerstatninger.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "edit": "allow"
  }
}
```

Dette verktÃ¸yet utfÃ¸rer nÃ¸yaktige redigeringer av filer ved Ã¥ erstatte eksakte teksttreff. Det er den primÃ¦re mÃ¥ten LLM endrer kode pÃ¥.

---

### write

Opprett nye filer eller overskriv eksisterende.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "edit": "allow"
  }
}
```

Bruk denne for Ã¥ la LLM lage nye filer. Den vil overskrive eksisterende filer hvis de allerede eksisterer.

:::note
`write`-verktÃ¸yet kontrolleres av tillatelsen `edit`, som dekker alle filendringer (`edit`, `write`, `patch`).
:::

---

### read

Les filinnholdet fra kodebasen din.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "read": "allow"
  }
}
```

Dette verktÃ¸yet leser filer og returnerer innholdet. Den stÃ¸tter lesing av spesifikke linjeomrÃ¥der for store filer.

---

### grep

SÃ¸k i filinnhold ved hjelp av regulÃ¦re uttrykk.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "grep": "allow"
  }
}
```

Rask innholdssÃ¸k pÃ¥ tvers av kodebasen din. StÃ¸tter full regex-syntaks og filmÃ¸nsterfiltrering.

---

### glob

Finn filer etter mÃ¸nstermatching.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "glob": "allow"
  }
}
```

SÃ¸k etter filer ved Ã¥ bruke glob-mÃ¸nstre som `**/*.js` eller `src/**/*.ts`. Returnerer samsvarende filbaner sortert etter endringstid.

---

### lsp (eksperimentell)

Samhandle med dine konfigurerte LSP-servere for Ã¥ fÃ¥ kodeintelligens-funksjoner som definisjoner, referanser, hover-informasjon og kallhierarki.

:::note
Dette verktÃ¸yet er bare tilgjengelig nÃ¥r `AO1_EXPERIMENTAL_LSP_TOOL=true` (eller `AO1_EXPERIMENTAL=true`).
:::

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "lsp": "allow"
  }
}
```

StÃ¸ttede operasjoner inkluderer `goToDefinition`, `findReferences`, `hover`, `documentSymbol`, `workspaceSymbol`, `goToImplementation`, `prepareCallHierarchy`, `incomingCalls` og `outgoingCalls`.

For Ã¥ konfigurere hvilke LSP-servere som er tilgjengelige for prosjektet ditt, se [LSP-servere](/docs/lsp).

---

### patch

Bruk patcher pÃ¥ filer.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "edit": "allow"
  }
}
```

Dette verktÃ¸yet bruker oppdateringsfiler til kodebasen din. Nyttig for Ã¥ pÃ¥fÃ¸re diffs og patcher fra forskjellige kilder.

:::note
`patch`-verktÃ¸yet kontrolleres av tillatelsen `edit`, som dekker alle filendringer (`edit`, `write`, `patch`).
:::

---

### skill

Last inn en [ferdighet](/docs/skills) (en `SKILL.md` fil) og returner innholdet i samtalen.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "skill": "allow"
  }
}
```

---

### todowrite

Administrer gjÃ¸remÃ¥lslister under kodingsÃ¸kter.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "todowrite": "allow"
  }
}
```

Oppretter og oppdaterer oppgavelister for Ã¥ spore fremdrift under komplekse operasjoner. LLM bruker dette til Ã¥ organisere flertrinnsoppgaver.

:::note
Dette verktÃ¸yet er deaktivert for subagenter som standard, men du kan aktivere det manuelt. [Finn ut mer](/docs/agents/#permissions)
:::

---

### webfetch

Hent nettinnhold.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "webfetch": "allow"
  }
}
```

Lar LLM hente og lese nettsider. Nyttig for Ã¥ slÃ¥ opp dokumentasjon eller forske pÃ¥ nettressurser.

---

### websearch

SÃ¸k pÃ¥ nettet for informasjon.

:::note
Dette verktÃ¸yet er bare tilgjengelig nÃ¥r du bruker OpenCode-leverandÃ¸ren eller nÃ¥r miljÃ¸variabelen `AO1_ENABLE_EXA` er satt til en sann verdi (f.eks. `true` eller `1`).

Slik aktiverer du nÃ¥r du starter OpenCode:

```bash
AO1_ENABLE_EXA=1 opencode
```

:::

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "websearch": "allow"
  }
}
```

UtfÃ¸rer nettsÃ¸k med Exa AI for Ã¥ finne relevant informasjon pÃ¥ nettet. Nyttig for Ã¥ undersÃ¸ke emner, finne aktuelle hendelser eller samle informasjon utover treningsdataavbruddet.

Ingen API-nÃ¸kkel kreves â€” verktÃ¸yet kobles direkte til Exa AIs vertsbaserte MCP-tjeneste uten autentisering.

:::tip
Bruk `websearch` nÃ¥r du trenger Ã¥ finne informasjon (oppdagelse), og `webfetch` nÃ¥r du trenger Ã¥ hente innhold fra en bestemt URL (henting).
:::

---

### question

Still brukeren spÃ¸rsmÃ¥l under utfÃ¸relse.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "question": "allow"
  }
}
```

Dette verktÃ¸yet lar LLM stille spÃ¸rsmÃ¥l til brukeren under en oppgave. Det er nyttig for:

- Samle brukerpreferanser eller krav
- Avklarende tvetydige instruksjoner
- FÃ¥ beslutninger om gjennomfÃ¸ringsvalg
- Tilbyr valg om hvilken retning du skal ta

Hvert spÃ¸rsmÃ¥l inkluderer en overskrift, spÃ¸rsmÃ¥lsteksten og en liste over alternativer. Brukere kan velge fra de angitte alternativene eller skrive inn et tilpasset svar. NÃ¥r det er flere spÃ¸rsmÃ¥l, kan brukerne navigere mellom dem fÃ¸r de sender inn alle svarene.

---

## Egendefinerte verktÃ¸y

Egendefinerte verktÃ¸y lar deg definere dine egne funksjoner som LLM kan kalle. Disse er definert i konfigurasjonsfilen din og kan kjÃ¸re vilkÃ¥rlig kode.

[Finn ut mer](/docs/custom-tools) om Ã¥ lage tilpassede verktÃ¸y.

---

## MCP servere

MCP (Model Context Protocol) servere lar deg integrere eksterne verktÃ¸y og tjenester. Dette inkluderer databasetilgang, API-integrasjoner og tredjepartstjenester.

[Les mer](/docs/mcp-servers) om konfigurering av MCP-servere.

---

## Internaler

Internt bruker verktÃ¸y som `grep` og `glob` [ripgrep](https://github.com/BurntSushi/ripgrep) i bakgrunnen. Som standard respekterer ripgrep `.gitignore`-mÃ¸nstre, noe som betyr at filer og kataloger som er oppfÃ¸rt i `.gitignore` vil bli ekskludert fra sÃ¸k og oppfÃ¸ringer.

---

### Ignorer mÃ¸nstre

For Ã¥ inkludere filer som normalt vil bli ignorert, lag en `.ignore`-fil i prosjektroten. Denne filen kan eksplisitt tillate visse stier.

```text title=".ignore"
!node_modules/
!dist/
!build/
```

For eksempel lar denne `.ignore` filen ripgrep sÃ¸ke i `node_modules/`, `dist/` og `build/` kataloger selv om de er oppfÃ¸rt i `.gitignore`.
