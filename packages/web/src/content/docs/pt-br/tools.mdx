---
title: Ferramentas
description: Gerencie as ferramentas que um LLM pode usar.
---

As ferramentas permitem que o LLM execute aÃ§Ãµes em sua base de cÃ³digo. O opencode vem com um conjunto de ferramentas integradas, mas vocÃª pode estendÃª-lo com [ferramentas personalizadas](/docs/custom-tools) ou [servidores MCP](/docs/mcp-servers).

Por padrÃ£o, todas as ferramentas estÃ£o **ativadas** e nÃ£o precisam de permissÃ£o para serem executadas. VocÃª pode controlar o comportamento das ferramentas atravÃ©s de [permissÃµes](/docs/permissions).

---

## ConfiguraÃ§Ã£o

Use o campo `permission` para controlar o comportamento das ferramentas. VocÃª pode permitir, negar ou exigir aprovaÃ§Ã£o para cada ferramenta.

```json title="opencode.json"
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "edit": "deny",
    "bash": "ask",
    "webfetch": "allow"
  }
}
```

VocÃª tambÃ©m pode usar curingas para controlar vÃ¡rias ferramentas ao mesmo tempo. Por exemplo, para exigir aprovaÃ§Ã£o para todas as ferramentas de um servidor MCP:

```json title="opencode.json"
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "mymcp_*": "ask"
  }
}
```

[Saiba mais](/docs/permissions) sobre como configurar permissÃµes.

---

## Integradas

Aqui estÃ£o todas as ferramentas integradas disponÃ­veis no opencode.

---

### bash

Execute comandos de shell no ambiente do seu projeto.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "bash": "allow"
  }
}
```

Esta ferramenta permite que o LLM execute comandos de terminal como `npm install`, `git status` ou qualquer outro comando de shell.

---

### edit

Modifique arquivos existentes usando substituiÃ§Ãµes de string exatas.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "edit": "allow"
  }
}
```

Esta ferramenta realiza ediÃ§Ãµes precisas em arquivos substituindo correspondÃªncias de texto exatas. Ã‰ a principal forma como o LLM modifica o cÃ³digo.

---

### write

Crie novos arquivos ou sobrescreva os existentes.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "edit": "allow"
  }
}
```

Use isso para permitir que o LLM crie novos arquivos. Ele sobrescreverÃ¡ arquivos existentes se jÃ¡ existirem.

:::note
A ferramenta `write` Ã© controlada pela permissÃ£o `edit`, que cobre todas as modificaÃ§Ãµes de arquivos (`edit`, `write`, `patch`).
:::

---

### read

Leia o conteÃºdo dos arquivos da sua base de cÃ³digo.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "read": "allow"
  }
}
```

Esta ferramenta lÃª arquivos e retorna seus conteÃºdos. Suporta a leitura de intervalos de linhas especÃ­ficos para arquivos grandes.

---

### grep

Pesquise o conteÃºdo dos arquivos usando expressÃµes regulares.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "grep": "allow"
  }
}
```

Busca rÃ¡pida de conteÃºdo em sua base de cÃ³digo. Suporta a sintaxe completa de regex e filtragem de padrÃµes de arquivos.

---

### glob

Encontre arquivos por correspondÃªncia de padrÃµes.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "glob": "allow"
  }
}
```

Pesquise arquivos usando padrÃµes glob como `**/*.js` ou `src/**/*.ts`. Retorna caminhos de arquivos correspondentes ordenados por tempo de modificaÃ§Ã£o.

---

### lsp (experimental)

Interaja com seus servidores LSP configurados para obter recursos de inteligÃªncia de cÃ³digo, como definiÃ§Ãµes, referÃªncias, informaÃ§Ãµes de hover e hierarquia de chamadas.

:::note
Esta ferramenta estÃ¡ disponÃ­vel apenas quando `AO1_EXPERIMENTAL_LSP_TOOL=true` (ou `AO1_EXPERIMENTAL=true`).
:::

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "lsp": "allow"
  }
}
```

As operaÃ§Ãµes suportadas incluem `goToDefinition`, `findReferences`, `hover`, `documentSymbol`, `workspaceSymbol`, `goToImplementation`, `prepareCallHierarchy`, `incomingCalls` e `outgoingCalls`.

Para configurar quais servidores LSP estÃ£o disponÃ­veis para seu projeto, veja [Servidores LSP](/docs/lsp).

---

### patch

Aplique patches a arquivos.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "edit": "allow"
  }
}
```

Esta ferramenta aplica arquivos de patch Ã  sua base de cÃ³digo. Ãštil para aplicar diffs e patches de vÃ¡rias fontes.

:::note
A ferramenta `patch` Ã© controlada pela permissÃ£o `edit`, que cobre todas as modificaÃ§Ãµes de arquivos (`edit`, `write`, `patch`).
:::

---

### skill

Carregue uma [skill](/docs/skills) (um arquivo `SKILL.md`) e retorne seu conteÃºdo na conversa.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "skill": "allow"
  }
}
```

---

### todowrite

Gerencie listas de tarefas durante sessÃµes de codificaÃ§Ã£o.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "todowrite": "allow"
  }
}
```

Cria e atualiza listas de tarefas para acompanhar o progresso durante operaÃ§Ãµes complexas. O LLM usa isso para organizar tarefas de mÃºltiplas etapas.

:::note
Esta ferramenta estÃ¡ desativada para subagentes por padrÃ£o, mas vocÃª pode ativÃ¡-la manualmente. [Saiba mais](/docs/agents/#permissions)
:::

---

### webfetch

Busque conteÃºdo da web.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "webfetch": "allow"
  }
}
```

Permite que o LLM busque e leia pÃ¡ginas da web. Ãštil para consultar documentaÃ§Ã£o ou pesquisar recursos online.

---

### websearch

Pesquise na web por informaÃ§Ãµes.

:::note
Esta ferramenta estÃ¡ disponÃ­vel apenas ao usar o provedor opencode ou quando a variÃ¡vel de ambiente `AO1_ENABLE_EXA` estÃ¡ definida como qualquer valor verdadeiro (por exemplo, `true` ou `1`).

Para habilitar ao iniciar o opencode:

```bash
AO1_ENABLE_EXA=1 opencode
```

:::

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "websearch": "allow"
  }
}
```

Realiza buscas na web usando Exa AI para encontrar informaÃ§Ãµes relevantes online. Ãštil para pesquisar tÃ³picos, encontrar eventos atuais ou reunir informaÃ§Ãµes alÃ©m do limite de dados de treinamento.

Nenhuma chave de API Ã© necessÃ¡ria â€” a ferramenta se conecta diretamente ao serviÃ§o MCP hospedado da Exa AI sem autenticaÃ§Ã£o.

:::tip
Use `websearch` quando precisar encontrar informaÃ§Ãµes (descoberta) e `webfetch` quando precisar recuperar conteÃºdo de uma URL especÃ­fica (recuperaÃ§Ã£o).
:::

---

### question

FaÃ§a perguntas ao usuÃ¡rio durante a execuÃ§Ã£o.

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "question": "allow"
  }
}
```

Esta ferramenta permite que o LLM faÃ§a perguntas ao usuÃ¡rio durante uma tarefa. Ã‰ Ãºtil para:

- Coletar preferÃªncias ou requisitos do usuÃ¡rio
- Esclarecer instruÃ§Ãµes ambÃ­guas
- Obter decisÃµes sobre escolhas de implementaÃ§Ã£o
- Oferecer opÃ§Ãµes sobre qual direÃ§Ã£o seguir

Cada pergunta inclui um cabeÃ§alho, o texto da pergunta e uma lista de opÃ§Ãµes. Os usuÃ¡rios podem selecionar entre as opÃ§Ãµes fornecidas ou digitar uma resposta personalizada. Quando hÃ¡ vÃ¡rias perguntas, os usuÃ¡rios podem navegar entre elas antes de enviar todas as respostas.

---

## Ferramentas personalizadas

Ferramentas personalizadas permitem que vocÃª defina suas prÃ³prias funÃ§Ãµes que o LLM pode chamar. Estas sÃ£o definidas em seu arquivo de configuraÃ§Ã£o e podem executar cÃ³digo arbitrÃ¡rio.

[Saiba mais](/docs/custom-tools) sobre como criar ferramentas personalizadas.

---

## Servidores MCP

Servidores MCP (Model Context Protocol) permitem que vocÃª integre ferramentas e serviÃ§os externos. Isso inclui acesso a bancos de dados, integraÃ§Ãµes de API e serviÃ§os de terceiros.

[Saiba mais](/docs/mcp-servers) sobre como configurar servidores MCP.

---

## Internos

Internamente, ferramentas como `grep` e `glob` usam [ripgrep](https://github.com/BurntSushi/ripgrep) por trÃ¡s dos panos. Por padrÃ£o, o ripgrep respeita padrÃµes `.gitignore`, o que significa que arquivos e diretÃ³rios listados em seu `.gitignore` serÃ£o excluÃ­dos de buscas e listagens.

---

### PadrÃµes de ignorar

Para incluir arquivos que normalmente seriam ignorados, crie um arquivo `.ignore` na raiz do seu projeto. Este arquivo pode permitir explicitamente certos caminhos.

```text title=".ignore"
!node_modules/
!dist/
!build/
```

Por exemplo, este arquivo `.ignore` permite que o ripgrep busque dentro dos diretÃ³rios `node_modules/`, `dist/` e `build/`, mesmo que estejam listados em `.gitignore`.
