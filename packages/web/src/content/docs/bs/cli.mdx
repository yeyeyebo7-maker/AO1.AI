---
title: CLI
description: OpenCode CLI opcije i naredbe.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

OpenCode CLI po defaultu pokreÄ‡e [TUI](/docs/tui) kada se pokrene bez ikakvih argumenata.

```bash
opencode
```

Ali takoÄ‘e prihvata naredbe kao Å¡to je dokumentovano na ovoj stranici. Ovo vam omoguÄ‡ava programsku interakciju sa OpenCode.

```bash
opencode run "Explain how closures work in JavaScript"
```

---

### tui

Pokrenite OpenCode terminalski korisniÄki interfejs.

```bash
opencode [project]
```

#### Opcije

| Opcija                                   | Kratko | Opis                                                                     |
| ---------------------------------------- | ------ | ------------------------------------------------------------------------ |
| <nobr><code>{"--continue"}</code></nobr> | `-c`   | Nastavite posljednju sesiju                                              |
| <nobr><code>{"--session"}</code></nobr>  | `-s`   | ID sesije za nastavak                                                    |
| <nobr><code>{"--fork"}</code></nobr>     |        | Forkujte sesiju pri nastavku (koristiti sa `--continue` ili `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |        | Prompt za upotrebu                                                       |
| <nobr><code>{"--model"}</code></nobr>    | `-m`   | Model za koriÅ¡tenje u obliku provider/model                              |
| <nobr><code>{"--agent"}</code></nobr>    |        | Agent za koriÅ¡tenje                                                      |
| <nobr><code>{"--port"}</code></nobr>     |        | Port na kojem treba sluÅ¡ati                                              |
| <nobr><code>{"--hostname"}</code></nobr> |        | Hostname na kojem treba sluÅ¡ati                                          |

---

## Naredbe

OpenCode CLI takoÄ‘e ima sljedeÄ‡e naredbe.

---

### agent

Upravljajte OpenCode agentima.

```bash
opencode agent [command]
```

---

### attach

PrikljuÄite terminal na veÄ‡ pokrenut OpenCode backend server pokrenut putem `serve` ili `web` naredbi.

```bash
opencode attach [url]
```

Ovo omoguÄ‡ava koriÅ¡tenje TUI-ja sa udaljenim OpenCode backend-om. Na primjer:

```bash
# Start the backend server for web/mobile access
opencode web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
opencode attach http://10.20.30.40:4096
```

#### Opcije

| Opcija                                   | Kratko | Opis                                                                                          |
| ---------------------------------------- | ------ | --------------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |        | Radni direktorij za pokretanje TUI-a                                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c`   | Nastavi posljednju sesiju                                                                     |
| <nobr><code>{"--session"}</code></nobr>  | `-s`   | ID sesije za nastavak                                                                         |
| <nobr><code>{"--fork"}</code></nobr>     |        | Forkuj sesiju prilikom nastavka (koristite sa `--continue` ili `--session`)                   |
| <nobr><code>{"--password"}</code></nobr> | `-p`   | Lozinka za osnovnu autentifikaciju (zadano: `AO1_SERVER_PASSWORD`)                       |
| <nobr><code>{"--username"}</code></nobr> | `-u`   | KorisniÄko ime za osnovnu autentifikaciju (zadano: `AO1_SERVER_USERNAME` ili `opencode`) |

---

#### create

Kreirajte novog agenta s prilagoÄ‘enom konfiguracijom.

```bash
opencode agent create
```

Ova naredba Ä‡e vas voditi kroz kreiranje novog agenta sa prilagoÄ‘enim sistemskim promptom i konfiguracijom alata.

---

#### list

Navedite sve dostupne agente.

```bash
opencode agent list
```

---

### auth

Naredba za upravljanje vjerodajnicama i prijavom za provajdere.

```bash
opencode auth [command]
```

---

#### login

OpenCode pokreÄ‡e lista provajdera na [Models.dev](https://models.dev), tako da moÅ¾ete koristiti `opencode auth login` da konfigurirate API kljuÄeve za bilo kojeg provajdera kojeg Å¾elite koristiti. Ovo je pohranjeno u `~/.local/share/opencode/auth.json`.

```bash
opencode auth login
```

Kada se OpenCode pokrene, uÄitava dobavljaÄe iz datoteke vjerodajnica. I ako postoje neki kljuÄevi definirani u vaÅ¡im okruÅ¾enjima ili `.env` fajl u vaÅ¡em projektu.

---

#### list

Navodi sve autentifikovane dobavljaÄe pohranjene u datoteci vjerodajnica.

```bash
opencode auth list
```

Ili kratka verzija.

```bash
opencode auth ls
```

---

#### logout

Odjavljuje vas s provajdera tako Å¡to ga briÅ¡e iz datoteke vjerodajnica.

```bash
opencode auth logout
```

---

### github

Upravljajte GitHub agentom za automatizaciju repozitorija.

```bash
opencode github [command]
```

---

#### install

Instalirajte GitHub agenta u svoj repozitorij.

```bash
opencode github install
```

Ovo postavlja neophodni tok rada GitHub Actions i vodi vas kroz proces konfiguracije. [Saznajte viÅ¡e](/docs/github).

---

#### run

Pokrenite GitHub agent. Ovo se obiÄno koristi u GitHub Actions.

```bash
opencode github run
```

##### Opcije

| Opcija                                | Opis                                   |
| ------------------------------------- | -------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub mock event za pokretanje agenta |
| <nobr><code>{"--token"}</code></nobr> | GitHub Personal Access Token           |

---

### mcp

Upravljajte Model Context Protocol (MCP) serverima.

```bash
opencode mcp [command]
```

---

#### add

Dodajte MCP server svojoj konfiguraciji.

```bash
opencode mcp add
```

Ova naredba Ä‡e vas voditi kroz dodavanje lokalnog ili udaljenog MCP servera.

---

#### list

Navedite sve konfigurirane MCP servere i njihov status veze.

```bash
opencode mcp list
```

Ili koristite kratku verziju.

```bash
opencode mcp ls
```

---

#### auth

Autentifikujte se sa MCP serverom koji je omoguÄ‡en za OAuth.

```bash
opencode mcp auth [name]
```

Ako ne navedete ime servera, od vas Ä‡e biti zatraÅ¾eno da izaberete neki od dostupnih servera koji podrÅ¾avaju OAuth.
TakoÄ‘er moÅ¾ete navesti servere koji podrÅ¾avaju OAuth i njihov status autentifikacije.

```bash
opencode mcp auth list
```

Ili koristite kratku verziju.

```bash
opencode mcp auth ls
```

---

#### logout

Uklonite OAuth vjerodajnice za MCP server.

```bash
opencode mcp logout [name]
```

---

#### debug

Otklanjanje greÅ¡aka (debug) OAuth veze sa MCP serverom.

```bash
opencode mcp debug <name>
```

---

### models

Navedite sve dostupne modele konfiguriranih provajdera.

```bash
opencode models [provider]
```

Ova naredba prikazuje sve modele dostupne kod vaÅ¡ih konfiguriranih provajdera u formatu `provider/model`.
Ovo je korisno za pronalaÅ¾enje taÄnog naziva modela za koriÅ¡tenje u [vaÅ¡oj konfiguraciji](/docs/config/).
Opciono moÅ¾ete proslijediti ID provajdera za filtriranje modela po tom dobavljaÄu.

```bash
opencode models anthropic
```

#### Opcije

| Opcija                                  | Opis                                                                     |
| --------------------------------------- | ------------------------------------------------------------------------ |
| <nobr><code>{"--refresh"}</code></nobr> | OsvjeÅ¾ite keÅ¡ modela sa models.dev                                       |
| <nobr><code>{"--verbose"}</code></nobr> | Koristite detaljniji izlaz modela (ukljuÄuje metapodatke poput troÅ¡kova) |

Koristite `--refresh` zastavicu da aÅ¾urirate keÅ¡iranu listu modela. Ovo je korisno kada su novi modeli dodani provajderu i Å¾elite da ih vidite u OpenCode.

```bash
opencode models --refresh
```

---

### run

Pokrenite OpenCode u neinteraktivnom modu tako Å¡to Ä‡ete direktno proslijediti prompt.

```bash
opencode run [message..]
```

Ovo je korisno za skriptiranje, automatizaciju ili kada Å¾elite brz odgovor bez pokretanja punog TUI-ja. Na primjer:

```bash "opencode run"
opencode run Explain the use of context in Go
```

TakoÄ‘er moÅ¾ete prikljuÄiti pokrenutu `opencode serve` instancu kako biste izbjegli vrijeme hladnog pokretanja MCP servera pri svakom pokretanju:

```bash
# Start a headless server in one terminal
opencode serve

# In another terminal, run commands that attach to it
opencode run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Opcije

| Opcija                                   | Kratko | Opis                                                                                          |
| ---------------------------------------- | ------ | --------------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |        | Naredba za pokretanje, koristite poruku za argumente                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c`   | Nastavite posljednju sesiju                                                                   |
| <nobr><code>{"--session"}</code></nobr>  | `-s`   | ID sesije za nastavak                                                                         |
| <nobr><code>{"--fork"}</code></nobr>     |        | Forkujte sesiju pri nastavku (koristiti sa `--continue` ili `--session`)                      |
| <nobr><code>{"--share"}</code></nobr>    |        | Podijelite sesiju                                                                             |
| <nobr><code>{"--model"}</code></nobr>    | `-m`   | Model za koriÅ¡tenje u obliku provider/model                                                   |
| <nobr><code>{"--agent"}</code></nobr>    |        | Agent za koriÅ¡tenje                                                                           |
| <nobr><code>{"--file"}</code></nobr>     | `-f`   | Fajlovi koje treba priloÅ¾iti poruci                                                           |
| <nobr><code>{"--format"}</code></nobr>   |        | Format: default (formatiran) ili json (sirovi JSON dogaÄ‘aji)                                  |
| <nobr><code>{"--title"}</code></nobr>    |        | Naslov sesije (koristi skraÄ‡eni prompt ako nije navedena vrijednost)                          |
| <nobr><code>{"--attach"}</code></nobr>   |        | PrikljuÄite na pokrenuti OpenCode server (npr. http://localhost:4096)                         |
| <nobr><code>{"--password"}</code></nobr> | `-p`   | Lozinka za osnovnu autentifikaciju (zadano: `AO1_SERVER_PASSWORD`)                       |
| <nobr><code>{"--username"}</code></nobr> | `-u`   | KorisniÄko ime za osnovnu autentifikaciju (zadano: `AO1_SERVER_USERNAME` ili `opencode`) |
| <nobr><code>{"--dir"}</code></nobr>      |        | Direktorij za pokretanje, ili putanja na udaljenom serveru pri spajanju                       |
| <nobr><code>{"--variant"}</code></nobr>  |        | Varijanta modela (napor zakljuÄivanja specifiÄan za provajdera)                               |
| <nobr><code>{"--thinking"}</code></nobr> |        | PrikaÅ¾i blokove razmiÅ¡ljanja                                                                  |
| <nobr><code>{"--port"}</code></nobr>     |        | Port za lokalni server (zadano na nasumiÄni port)                                             |

---

### serve

Pokrenite OpenCode headless server za API pristup. Pogledajte [server docs](/docs/server) za kompletan HTTP interfejs.

```bash
opencode serve
```

Ovo pokreÄ‡e HTTP server koji pruÅ¾a API pristup funkcionalnosti OpenCode-a bez TUI interfejsa. Postavite `AO1_SERVER_PASSWORD` da omoguÄ‡ite HTTP osnovnu auth (korisniÄko ime je zadano na `opencode`).

#### Opcije

| Opcija                                   | Opis                                                  |
| ---------------------------------------- | ----------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Port na kojem treba sluÅ¡ati                           |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname na kojem treba sluÅ¡ati                       |
| <nobr><code>{"--mdns"}</code></nobr>     | OmoguÄ‡i mDNS otkrivanje                               |
| <nobr><code>{"--cors"}</code></nobr>     | Dodatni origin(i) pretraÅ¾ivaÄa koji dozvoljavaju CORS |

---

### session

Upravljajte OpenCode sesijama.

```bash
opencode session [command]
```

---

#### list

Navedite sve OpenCode sesije.

```bash
opencode session list
```

##### Opcije

| Opcija                                    | Kratko | Opis                                   |
| ----------------------------------------- | ------ | -------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`   | OgraniÄenje na N najnovijih sesija     |
| <nobr><code>{"--format"}</code></nobr>    |        | Izlazni format: table ili json (table) |

---

### stats

PrikaÅ¾i statistiku upotrebe tokena i troÅ¡kova za vaÅ¡e OpenCode sesije.

```bash
opencode stats
```

#### Opcije

| Opcija                                  | Opis                                                                                                       |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | PrikaÅ¾i statistiku za zadnjih N dana (sva vremena)                                                         |
| <nobr><code>{"--tools"}</code></nobr>   | Broj alata za prikaz (svi)                                                                                 |
| <nobr><code>{"--models"}</code></nobr>  | PrikaÅ¾i raÅ¡Älambu koriÅ¡tenja modela (skriveno prema zadanim postavkama). Proslijedite broj za prikaz top N |
| <nobr><code>{"--project"}</code></nobr> | Filtriraj po projektu (svi projekti, prazan niz: trenutni projekt)                                         |

---

### export

Izvezite podatke sesije kao JSON.

```bash
opencode export [sessionID]
```

Ako ne unesete ID sesije, od vas Ä‡e biti zatraÅ¾eno da odaberete neku od dostupnih sesija.

---

### import

Uvezite podatke sesije iz JSON datoteke ili OpenCode dijeljenog URL-a.

```bash
opencode import <file>
```

MoÅ¾ete uvesti iz lokalne datoteke ili OpenCode dijeljenog URL-a.

```bash
opencode import session.json
opencode import https://opncd.ai/s/abc123
```

---

### web

Pokrenite OpenCode headless server sa web interfejsom.

```bash
opencode web
```

Ovo pokreÄ‡e HTTP server i otvara web pretraÅ¾ivaÄ za pristup OpenCode-u preko web interfejsa. Postavite `AO1_SERVER_PASSWORD` da omoguÄ‡ite HTTP osnovnu auth (korisniÄko ime je zadano na `opencode`).

#### Opcije

| Opcija                                   | Opis                                                  |
| ---------------------------------------- | ----------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Port na kojem treba sluÅ¡ati                           |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname na kojem treba sluÅ¡ati                       |
| <nobr><code>{"--mdns"}</code></nobr>     | OmoguÄ‡i mDNS otkrivanje                               |
| <nobr><code>{"--cors"}</code></nobr>     | Dodatni origin(i) pretraÅ¾ivaÄa koji dozvoljavaju CORS |

---

### acp

Pokrenite ACP (Agent Client Protocol) server.

```bash
opencode acp
```

Ova naredba pokreÄ‡e ACP server koji komunicira preko stdin/stdout koristeÄ‡i nd-JSON.

#### Opcije

| Opcija                                   | Opis                        |
| ---------------------------------------- | --------------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Radni direktorij            |
| <nobr><code>{"--port"}</code></nobr>     | Port na kojem treba sluÅ¡ati |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname na kojem sluÅ¡ati   |

---

### uninstall

Deinstalirajte OpenCode i uklonite sve povezane datoteke.

```bash
opencode uninstall
```

#### Opcije

| Opcija                                      | Kratko | Opis                                          |
| ------------------------------------------- | ------ | --------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`   | SaÄuvajte konfiguracijske datoteke            |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`   | SaÄuvajte podatke i snimke sesije             |
| <nobr><code>{"--dry-run"}</code></nobr>     |        | PokaÅ¾ite Å¡ta bi bilo uklonjeno bez uklanjanja |
| <nobr><code>{"--force"}</code></nobr>       | `-f`   | PreskoÄi upite za potvrdu                     |

---

### upgrade

AÅ¾urira OpenCode na najnoviju verziju ili odreÄ‘enu verziju.

```bash
opencode upgrade [target]
```

Za nadogradnju na najnoviju verziju.

```bash
opencode upgrade
```

Za nadogradnju na odreÄ‘enu verziju.

```bash
opencode upgrade v0.1.48
```

#### Opcije

| Opcija                                 | Kratko | Opis                                                    |
| -------------------------------------- | ------ | ------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`   | KoriÅ¡teni naÄin instalacije; curl, npm, pnpm, bun, brew |

---

## Globalne opcije

OpenCode CLI prihvata sljedeÄ‡e globalne zastavice.

| Opcija                                     | Kratko | Opis                                      |
| ------------------------------------------ | ------ | ----------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h`   | PrikaÅ¾i pomoÄ‡                             |
| <nobr><code>{"--version"}</code></nobr>    | `-v`   | IspiÅ¡i broj verzije                       |
| <nobr><code>{"--print-logs"}</code></nobr> |        | Ispis logova u stderr                     |
| <nobr><code>{"--log-level"}</code></nobr>  |        | Nivo logovanja (DEBUG, INFO, WARN, ERROR) |

---

## Varijable okruÅ¾enja

OpenCode se moÅ¾e konfigurirati pomoÄ‡u varijabli okruÅ¾enja.

| Varijabla                             | Tip     | Opis                                                               |
| ------------------------------------- | ------- | ------------------------------------------------------------------ |
| `AO1_AUTO_SHARE`                 | boolean | Automatski dijeli sesije                                           |
| `AO1_GIT_BASH_PATH`              | string  | Putanja do Git Bash izvrÅ¡ne datoteke na Windows-u                  |
| `AO1_CONFIG`                     | string  | Putanja do konfiguracijskog fajla                                  |
| `AO1_TUI_CONFIG`                 | string  | Putanja do TUI konfiguracijskog fajla                              |
| `AO1_CONFIG_DIR`                 | string  | Putanja do konfiguracijskog direktorija                            |
| `AO1_CONFIG_CONTENT`             | string  | Inline json konfiguracijski sadrÅ¾aj                                |
| `AO1_DISABLE_AUTOUPDATE`         | boolean | OnemoguÄ‡i automatske provjere aÅ¾uriranja                           |
| `AO1_DISABLE_PRUNE`              | boolean | OnemoguÄ‡i brisanje (pruning) starih podataka                       |
| `AO1_DISABLE_TERMINAL_TITLE`     | boolean | OnemoguÄ‡i automatsko aÅ¾uriranje naslova terminala                  |
| `AO1_PERMISSION`                 | string  | Inline json konfiguracija dozvola                                  |
| `AO1_DISABLE_DEFAULT_PLUGINS`    | boolean | OnemoguÄ‡i podrazumijevane dodatke (plugins)                        |
| `AO1_DISABLE_LSP_DOWNLOAD`       | boolean | OnemoguÄ‡i automatsko preuzimanje LSP servera                       |
| `AO1_ENABLE_EXPERIMENTAL_MODELS` | boolean | OmoguÄ‡i eksperimentalne modele                                     |
| `AO1_DISABLE_AUTOCOMPACT`        | boolean | OnemoguÄ‡i automatsko saÅ¾imanje konteksta                           |
| `AO1_DISABLE_CLAUDE_CODE`        | boolean | OnemoguÄ‡i Äitanje iz `.claude` (prompt + vjeÅ¡tine)                 |
| `AO1_DISABLE_CLAUDE_CODE_PROMPT` | boolean | OnemoguÄ‡i Äitanje `~/.claude/CLAUDE.md`                            |
| `AO1_DISABLE_CLAUDE_CODE_SKILLS` | boolean | OnemoguÄ‡i uÄitavanje `.claude/skills`                              |
| `AO1_DISABLE_MODELS_FETCH`       | boolean | OnemoguÄ‡i dohvaÄ‡anje modela iz udaljenih izvora                    |
| `AO1_FAKE_VCS`                   | string  | LaÅ¾ni VCS provajder za potrebe testiranja                          |
| `AO1_CLIENT`                     | string  | Identifikator klijenta (zadano na `cli`)                           |
| `AO1_ENABLE_EXA`                 | boolean | OmoguÄ‡i Exa alate za web pretraÅ¾ivanje                             |
| `AO1_SERVER_PASSWORD`            | string  | OmoguÄ‡i osnovnu autentifikaciju za `serve`/`web`                   |
| `AO1_SERVER_USERNAME`            | string  | PoniÅ¡ti osnovno korisniÄko ime autentifikacije (zadano `opencode`) |
| `AO1_MODELS_URL`                 | string  | PrilagoÄ‘eni URL za dohvaÄ‡anje konfiguracije modela                 |

---

### Eksperimentalno

Ove varijable okruÅ¾enja omoguÄ‡avaju eksperimentalne karakteristike koje se mogu promijeniti ili ukloniti.

| Varijabla                                       | Tip     | Opis                                                    |
| ----------------------------------------------- | ------- | ------------------------------------------------------- |
| `AO1_EXPERIMENTAL`                         | boolean | OmoguÄ‡i eksperimentalne funkcije pod zbirnom zastavicom |
| `AO1_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | OmoguÄ‡i otkrivanje ikona                                |
| `AO1_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | OnemoguÄ‡i kopiranje pri odabiru u TUI                   |
| `AO1_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Zadano vremensko ograniÄenje za bash naredbe u ms       |
| `AO1_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Maksimalni izlazni tokeni za LLM odgovore               |
| `AO1_EXPERIMENTAL_FILEWATCHER`             | boolean | OmoguÄ‡i praÄ‡enje datoteka za cijeli direktorij          |
| `AO1_EXPERIMENTAL_OXFMT`                   | boolean | OmoguÄ‡i oxfmt formatter                                 |
| `AO1_EXPERIMENTAL_LSP_TOOL`                | boolean | OmoguÄ‡i eksperimentalni LSP alat                        |
| `AO1_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | OnemoguÄ‡i praÄ‡enje datoteka                             |
| `AO1_EXPERIMENTAL_EXA`                     | boolean | OmoguÄ‡i eksperimentalne Exa funkcije                    |
| `AO1_EXPERIMENTAL_LSP_TY`                  | boolean | OmoguÄ‡i TY LSP za python datoteke                       |
| `AO1_EXPERIMENTAL_PLAN_MODE`               | boolean | OmoguÄ‡i Plan mod                                        |
| `AO1_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | OmoguÄ‡i pozadinske zadatke subagenata                   |
| `AO1_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | OmoguÄ‡i eksperimentalni sistem dogaÄ‘aja                 |
| `AO1_EXPERIMENTAL_NATIVE_LLM`              | boolean | OmoguÄ‡i nativnu putanju LLM zahtjeva                    |
| `AO1_EXPERIMENTAL_PARALLEL`                | boolean | OmoguÄ‡i paralelno izvrÅ¡avanje web pretrage              |
| `AO1_EXPERIMENTAL_SCOUT`                   | boolean | OmoguÄ‡i Scout subagenta                                 |
| `AO1_EXPERIMENTAL_WORKSPACES`              | boolean | OmoguÄ‡i podrÅ¡ku za radne prostore                       |
